name: Trigger Organisation AI Edit

on:
  issues:
    types:
      - labeled

permissions:
  contents: write
  pull-requests: write
  issues: write

jobs:
  call-minimax-agent:
    if: github.event.label.name == 'ai-edit'

    uses: CafeGlow/.GITHUB/.github/workflows/ai-edit.yml@main

    secrets: inherit
