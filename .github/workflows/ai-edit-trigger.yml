name: Trigger Central AI Edit Workflow

on:
  issues:
    types:
      - labeled

permissions:
  contents: write
  pull-requests: write
  issues: write

jobs:
  call-central-ai-edit:
    if: github.event.label.name == 'ai-edit'

    uses: CafeGlow/.GITHUB/.github/workflows/ai-edit.yml@main

    secrets: inherit
